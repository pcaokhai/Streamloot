{
  "name": "streamloot-extension",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "description": "Streamloot browser extension — currently ADR 0005 Stage 0 probe only",
  "scripts": {
    "dev": "wxt",
    "build": "wxt build",
    "zip": "wxt zip",
    "compile": "tsc --noEmit",
    "postinstall": "wxt prepare"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "wxt": "^0.20.0"
  }
}
